---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: oidcclients.auth.kettleofketchup
spec:
  group: auth.kettleofketchup
  names:
    kind: OIDCClient
    listKind: OIDCClientList
    plural: oidcclients
    shortNames:
    - oidc
    singular: oidcclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.authentik.applicationSlug
      name: Slug
      type: string
    - jsonPath: .spec.secretProfile
      name: Profile
      type: string
    - jsonPath: .spec.target.namespace
      name: Target NS
      type: string
    - jsonPath: .status.conditions[?(@.type=="AuthentikProviderFound")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="SecretSynced")].status
      name: Synced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OIDCClient is the Schema for the oidcclients API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OIDCClientSpec defines the desired state of OIDCClient
            properties:
              authentik:
                description: Authentik defines which Authentik application to read
                  from
                properties:
                  applicationSlug:
                    description: ApplicationSlug is the slug of the Authentik application
                    minLength: 1
                    type: string
                required:
                - applicationSlug
                type: object
              configMapTarget:
                description: ConfigMapTarget patches a ConfigMap with a profile-generated
                  value
                properties:
                  dataKey:
                    description: DataKey is the key inside the ConfigMap's data to
                      write to
                    type: string
                  name:
                    description: Name of the ConfigMap to patch
                    type: string
                  namespace:
                    description: Namespace of the ConfigMap
                    type: string
                  sourceKey:
                    description: SourceKey is the profile-generated secret key to
                      read the value from
                    type: string
                required:
                - dataKey
                - name
                - namespace
                - sourceKey
                type: object
              rolloutRestart:
                description: RolloutRestart configures automatic workload restart
                  on secret changes
                properties:
                  enabled:
                    default: false
                    description: Enabled controls whether rollout restart is active
                    type: boolean
                  targetRef:
                    description: TargetRef identifies the workload to restart
                    properties:
                      kind:
                        description: Kind of the target (Deployment or StatefulSet)
                        enum:
                        - Deployment
                        - StatefulSet
                        type: string
                      name:
                        description: Name of the target resource
                        type: string
                      namespace:
                        description: Namespace of the target resource
                        type: string
                    required:
                    - kind
                    - name
                    - namespace
                    type: object
                required:
                - enabled
                type: object
              secretOverrides:
                additionalProperties:
                  type: string
                description: SecretOverrides adds or overrides keys in the generated
                  secret
                type: object
              secretProfile:
                default: generic
                description: SecretProfile selects a built-in key mapping profile
                enum:
                - grafana
                - openwebui
                - argocd
                - ragflow
                - generic
                type: string
              target:
                description: Target defines where to write the OIDC credentials
                properties:
                  namespace:
                    description: Namespace to create the secret in
                    type: string
                  secretName:
                    description: SecretName is the name of the Secret to create/update
                    type: string
                required:
                - namespace
                - secretName
                type: object
            required:
            - authentik
            - secretProfile
            - target
            type: object
          status:
            description: OIDCClientStatus defines the observed state of OIDCClient
            properties:
              conditions:
                description: Conditions represent the latest observations of the resource's
                  state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the operator synced with
                  Authentik
                format: date-time
                type: string
              secretHash:
                description: SecretHash is the SHA256 hash of the current secret data
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
